package com.taskflow.common;

import org.springframework.context.annotation.Configuration;

@Configuration
public class MapperConfig {
    // Shared mapper definitions and converter registry placeholders
}
