package com.taskflow.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class JpaAuditingConfig {
    // Audit configuration triggers handled via @EnableJpaAuditing on TaskFlowApplication
}
